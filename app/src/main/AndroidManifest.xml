<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thomosim.consentcoin">

    <!--TODO Create a theme for the entire app. Or maybe multiple themes and allow users to switch between them via a settings menu-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LightTheme">
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.ProcessRequestActivity" />
        <activity android:name=".View.MyConsentcoinActivity" />
        <activity android:name=".View.ProcessInviteActivity" />
        <activity android:name=".View.CreateRequestActivity" />
    </application>

</manifest>